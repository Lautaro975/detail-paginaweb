import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Iniciar sesión",
    description: "Iniciar sesión",
}

export default function Login() {
    return (
        <div className="text-[#000]">
            hola estoy en el login
        </div>
    );
}