import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Carrito",
    description: "Carrito",
}

export default function Cart() {
    return (
        <div>Carrito</div>
    );
}